{
  "name": "find-the-corgi",
  "version": "1.0.0",
  "description": "Find the Corgi - 1000 Words English-Thai Web App",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "vite build && esbuild server.js --bundle --platform=node --format=cjs --packages=external --sourcemap --outfile=dist/server.cjs",
    "preview": "vite preview",
    "start": "node dist/server.cjs"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "esbuild": "^0.28.1",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "@google/genai": "^2.12.0",
    "express": "^5.2.1",
    "firebase": "^12.16.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "recharts": "^3.9.2"
  }
}
